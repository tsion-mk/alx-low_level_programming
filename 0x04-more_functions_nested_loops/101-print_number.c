#include "main.h"

/**
 * print_number - prints an integer
 * @n: number to be printeid
 */

void print_number(int n)
{
	if (n < 0)
	{
		_putchar('-');
		_putchar('0' + n * -1);
	}
	else if (n > 9)
	{
		while ()
		{
			_putchar('0' + 
	else
		_putchar('0' + n);
}
