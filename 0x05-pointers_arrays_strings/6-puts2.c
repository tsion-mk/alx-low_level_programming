#include "main.h"

/**
 * puts2 - prints every character of a string, starting from first character
 * @str: a string
 */

void puts2(char *str)
{
	int i;

	i = 0;
	while(str[i] != '\0')
	{
		_putchar('0' + );
		i = i + 2;
	}
	_putchar('\n');
}

